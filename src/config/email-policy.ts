/** Internal fixed reason stored on scheduled_messages cancelled by policy. */
export const OUTBOUND_EMAIL_POLICY_CANCELLATION_REASON =
  "Cancelled by outbound email policy: transactional-only launch";
